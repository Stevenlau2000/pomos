# POMOS 后端依赖清单（Python 3.11+）
# 安装：pip install -r requirements.txt

fastapi>=0.110
uvicorn[standard]>=0.27
langgraph>=0.2
openai>=1.30
# Claude 原生 SDK（懒加载：未安装时 Claude 供应商不可用，但不影响导入与其他供应商）
anthropic>=0.40
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
redis>=5.0
